// 严格模式：处理看似正常，但实际是有问题的代码
// "use strict";

// 一、复制原始类型的值
// 直接把原始类型变量的值，拷贝给另一个变量即可，两个变量值虽然一样，但内存空间完全独立。（可以把原始类型的值看作副本，复制的副本）

// 二、复制对象类型的值
// 将对象类型赋值然后拷贝给另外一个变量，其实复制的引用地址。它们两最终指向同一个内存地址

// 这里是很基础的东西，就不举例了
